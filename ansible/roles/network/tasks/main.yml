---
- name: Create Private Network (DB <-> Backend)
  docker_network:
    name: private-network
    driver: bridge
    state: present

- name: Create Front Network (Backend <-> HTTPD)
  docker_network:
    name: front-network
    driver: bridge
    state: present

